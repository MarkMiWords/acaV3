{
  "name": "acav3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run --prefix functions build",
    "serve": "npm run --prefix functions build && firebase emulators:start --only functions,hosting",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "firebase-tools": "^15.5.1"
  }
}
